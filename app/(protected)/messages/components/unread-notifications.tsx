const UnreadNotifications = () => {
  return (
    // perfect circle with number in center and blue background
    <div className="w-2 h-2 rounded-full bg-red-500 flex items-center justify-center"></div>
  );
};

export default UnreadNotifications;
